# Production Configuration for Heroku
server.port=${PORT:8080}

# MongoDB Atlas (Cloud Database)
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/cardshop}

# Email Configuration (Production)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:nedimgnr53@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:ccgq ewwu zmny cchz}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Email Settings
app.email.from=${EMAIL_FROM:nedimgnr53@gmail.com}
app.email.enabled=${EMAIL_ENABLED:true}

# Logging
logging.level.com.nguner.yeditepecardshop=INFO
logging.level.org.springframework.security=WARN
